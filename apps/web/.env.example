# Web server binding
HOSTNAME=0.0.0.0
PORT=3001

# Search indexing is disabled by default.
# Enable only for an approved public production deployment.
SEARCH_INDEXING_ENABLED=false

# Server-side API origin used by the same-origin /api reverse proxy.
API_INTERNAL_ORIGIN=http://127.0.0.1:3000
